# Copy to .env.local for local development
VITE_API_URL=https://github-docs-worker.YOUR_SUBDOMAIN.workers.dev

# In production (Cloudflare Pages), set this in the Pages dashboard:
# Settings → Environment Variables
# VITE_API_URL=https://github-docs-worker.YOUR_SUBDOMAIN.workers.dev
